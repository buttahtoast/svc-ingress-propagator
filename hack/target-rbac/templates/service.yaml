apiVersion: v1
kind: Service
metadata:
  name: {{ include "target-rbac.fullname" . }}
  labels:
    {{- include "target-rbac.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "target-rbac.selectorLabels" . | nindent 4 }}
